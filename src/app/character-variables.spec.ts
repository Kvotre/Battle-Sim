import { CharacterVariables } from './character-variables';

describe('FirstCharacterVariables', () => {
  it('should create an instance', () => {
    expect(new CharacterVariables()).toBeTruthy();
  });
});
